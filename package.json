{
  "name": "@continua-ai/happy-paths",
  "version": "0.1.0",
  "description": "Trace-driven learning loop for coding agents: capture, index, mine wrong turns, and share happy paths.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "happy-paths": "dist/cli/happy-paths.js"
  },
  "files": ["dist", "extensions", "README.md", "LICENSE"],
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "bun@1.2.22",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "format": "biome format --write .",
    "guardrails": "tsx scripts/check-large-source-files.ts && tsx scripts/check-no-authored-js.ts && tsx scripts/check-no-sleeps.ts",
    "lint": "biome check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:wrong-turn-gate": "vitest run tests/wrong-turn-dataset.test.ts",
    "eval:wrong-turn": "tsc -p tsconfig.build.json && tsx scripts/run-wrong-turn-eval.ts --strict",
    "eval:feasibility": "tsc -p tsconfig.build.json && tsx scripts/run-feasibility-gate.ts --strict",
    "eval:skateboard": "tsc -p tsconfig.build.json && tsx scripts/run-feasibility-gate.ts --strict --bootstrap-samples 4000 --confidence-level 0.95",
    "eval:observed-ab": "tsc -p tsconfig.build.json && tsx scripts/run-observed-ab-gate.ts --strict",
    "eval:observed-ab:long-horizon": "tsc -p tsconfig.build.json && tsx scripts/run-observed-ab-long-horizon.ts --strict",
    "eval:trajectory-outcome:long-horizon": "tsc -p tsconfig.build.json && tsx scripts/run-trajectory-outcome-long-horizon.ts --strict",
    "eval:trajectory-calibration-sample": "tsc -p tsconfig.build.json && tsx scripts/build-trajectory-calibration-sample.ts",
    "eval:trajectory-calibration:prepare-dual-review": "tsc -p tsconfig.build.json && tsx scripts/prepare-trajectory-calibration-dual-review.ts",
    "eval:trajectory-calibration:adjudicate": "tsc -p tsconfig.build.json && tsx scripts/adjudicate-trajectory-calibration.ts",
    "eval:trajectory-calibration-summary": "tsc -p tsconfig.build.json && tsx scripts/summarize-trajectory-calibration.ts",
    "eval:trajectory-calibration:tune-thresholds": "tsc -p tsconfig.build.json && tsx scripts/tune-trajectory-calibration-thresholds.ts",
    "build:feasibility-dataset": "tsc -p tsconfig.build.json && tsx scripts/build-feasibility-dataset-from-traces.ts",
    "memo:feasibility": "tsc -p tsconfig.build.json && tsx scripts/write-feasibility-memo.ts",
    "sync:evidence-web": "tsx scripts/publish-feasibility-evidence.ts",
    "ingest:server": "tsx scripts/run-ingest-server.ts",
    "ingest:ship": "tsx scripts/ship-trace-bundles.ts",
    "loop:con1469": "tsx scripts/run-con1469-close-loop.ts",
    "loop:con1469:sweep-roots": "tsx scripts/sweep-con1469-long-horizon-roots.ts",
    "typecheck": "tsc --noEmit",
    "verify": "biome check . && tsc --noEmit && vitest run",
    "verify:ci": "biome check . && tsc --noEmit && vitest run && npm run guardrails"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/continua-ai/happy-paths.git"
  },
  "bugs": {
    "url": "https://github.com/continua-ai/happy-paths/issues"
  },
  "homepage": "https://github.com/continua-ai/happy-paths#readme",
  "keywords": ["agent", "llm", "retrieval", "trace", "pi", "continua"],
  "dependencies": {
    "@google-cloud/storage": "^7.16.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
