{
  "name": "@continua-ai/contilore",
  "version": "0.1.0",
  "description": "Trace-driven learning loop for coding agents: capture, index, mine wrong turns, and share happy paths.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "format": "biome format --write .",
    "guardrails": "node scripts/check-large-source-files.mjs",
    "lint": "biome check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:wrong-turn-gate": "vitest run tests/wrong-turn-dataset.test.ts",
    "eval:wrong-turn": "npm run build && node scripts/run-wrong-turn-eval.mjs --strict",
    "typecheck": "tsc --noEmit",
    "verify": "npm run lint && npm run typecheck && npm run test",
    "verify:ci": "npm run verify && npm run guardrails"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/continua-ai/contilore.git"
  },
  "bugs": {
    "url": "https://github.com/continua-ai/contilore/issues"
  },
  "homepage": "https://github.com/continua-ai/contilore#readme",
  "keywords": ["agent", "llm", "retrieval", "trace", "pi", "continua"],
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
